/**
 * GrainOverlay - Fixed noise texture overlay
 * Pure CSS component, no client-side logic needed.
 */
export function GrainOverlay() {
  return (
    <div
      className="grain"
      aria-hidden="true"
    />
  );
}
