{
  "name": "mirai-new-site",
  "private": true,
  "type": "module",
  "description": "Static site plus one Vercel function: Stripe webhook that issues Luma tickets.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "stripe": "^22.6.2"
  }
}
